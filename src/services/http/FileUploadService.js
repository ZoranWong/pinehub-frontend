import ApiService from './ApiService';
export default class FileUploadService extends ApiService{
	constructor(application) {
		super(application);
	}
	async upLoad(page = 1, search = null, limit = 15) {
//		ADMIN_SERVER_HOST+'/app/logo/cloud?token='+tokens"
	}
}
